<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <!-- Note this should be the (2 * padding + textSize) / 2 for the text view-->
    <corners
        android:radius="20dp" />
    <stroke
        android:color="@color/pastelRose"
        android:width="1dp" />
</shape>